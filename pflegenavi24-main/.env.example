# Example environment variables
# For public (non-secret) values you can use VITE_ prefix to expose them to the frontend.
# For secret keys, do NOT use VITE_ prefix — instead set them in Netlify environment variables and call them from server-side functions.

VITE_GEMINI_API_KEY=your_public_key_here
